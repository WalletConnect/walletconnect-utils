name: "CTA Assistant"
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

# Required permissions for CTA Assistant workflow
permissions:
  actions: write
  contents: write # can be 'read' if signatures are in remote repository
  pull-requests: write
  statuses: write

jobs:
  CTA:
    runs-on: ubuntu-latest
    steps:
      - uses: WalletConnect/actions/github/cta-assistant@d6395999d484da4e542b83d345532c284ced539c
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
