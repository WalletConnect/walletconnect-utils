import "mocha";
import * as chai from "chai";
import chaiAsPromised from "chai-as-promised";

chai.use(chaiAsPromised);

describe("@walletconnect/jsonrpc-http-connection", () => {
  it("needs tests", async () => {
    // needs tests
  });
});
